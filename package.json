{
  "name": "karisuke",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "nx:graph": "nx graph",
    "restore": "nx run-many -t restore",
    "build": "nx run-many -t build",
    "test": "nx run-many -t test",
    "affected:build": "nx affected -t build",
    "affected:test": "nx affected -t test",
    "docker:build": "nx run-many -t docker-build --projects=gateway,tb-publisher,tb-consumer,rules-api"
  },
  "devDependencies": {
    "@nx/dotnet": "23.0.1",
    "nx": "23.0.1"
  }
}

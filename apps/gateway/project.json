{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "gateway",
  "targets": {
    "docker-build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker build -f apps/gateway/Dockerfile -t karisuke/gateway:local .",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}

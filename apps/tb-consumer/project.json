{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "tb-consumer",
  "targets": {
    "docker-build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker build -f apps/tb-consumer/Dockerfile -t karisuke/tb-consumer:local .",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}

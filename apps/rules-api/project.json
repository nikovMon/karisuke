{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "rules-api",
  "targets": {
    "docker-build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker build -f apps/rules-api/Dockerfile -t karisuke/rules-api:local .",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
